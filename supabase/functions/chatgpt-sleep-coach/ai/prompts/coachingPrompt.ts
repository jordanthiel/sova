/**
 * Phase 2 — stub string for future coaching orchestrator.
 */
export function coachingPromptStub(): string {
  return `You are a warm baby sleep coach. Phase 2 coaching orchestrator will supply structured context.
For now respond conversationally without inventing new logged events.`;
}
